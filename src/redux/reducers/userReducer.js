import { SET_SC_USER } from "../actions/types";

const initialState = {
  scUser: null
};

export default function(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
